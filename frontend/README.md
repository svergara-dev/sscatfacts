# React + TypeScript + Vite

This template provides a minimal setup to get React working in Vite with HMR.

## ESLint + Prettier

This project uses ESLint v10 with flat config and Prettier for code formatting.

Run linting:
```bash
npm run lint
```
